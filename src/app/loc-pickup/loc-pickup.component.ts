import {Component, EventEmitter, OnInit} from '@angular/core';
// import {FormBuilder, FormGroup, FormControl} from '@angular/forms';

@Component({
  selector: 'app-loc-pickup',
  templateUrl: './loc-pickup.component.html',
  styleUrls: ['./loc-pickup.component.css'],
  // providers: [FormBuilder]
})
export class LocPickupComponent implements OnInit {

  constructor() { }

  ngOnInit() {
  }

}
